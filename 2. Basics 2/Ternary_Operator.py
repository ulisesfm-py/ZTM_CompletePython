#Ternary Operator

is_friend = True
can_message = "message allowed" if is_friend else "not allowed to message"

print(can_message)

Age = 11
can_vote = "allowed to vote" if Age>=18 else "not allowed to vote"

print(can_vote)

